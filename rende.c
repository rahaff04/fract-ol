#include "fractol.h"

void  handle_pixil(int x, int y, t_fractol *f)
{
       //pp
}
void fractol_render(t_fractol *f)
{
    int x;
    int y;

    y = -1;
    while (++y < HEIGHT)
    {
        x = -1;
        while (++x < WIDTH)
        {
            //handle_pixil(f, x, y);
        }
    }
}